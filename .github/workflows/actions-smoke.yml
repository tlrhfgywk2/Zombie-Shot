name: Actions Smoke Check

on:
  push:
    branches: [develop]
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Verify runner startup
        run: echo "GitHub Actions runner started successfully"
